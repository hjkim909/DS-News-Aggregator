# DS News Aggregator - Environment Variables
# 이 파일을 .env로 복사하고 실제 값으로 설정하세요

# =============================================================================
# 필수 API 키들 (반드시 설정 필요)
# =============================================================================

# Reddit API 설정 (무료) - https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=your-reddit-client-id
REDDIT_CLIENT_SECRET=your-reddit-client-secret
REDDIT_USER_AGENT=DSNewsAggregator/1.0

# Google Gemini API 설정 (요약용) - https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-pro

# =============================================================================
# Flask 웹앱 설정
# =============================================================================

# Flask 보안 키 (배포시 반드시 변경)
SECRET_KEY=change-this-secret-key-in-production-use-random-string

# 서버 설정
PORT=5000
FLASK_ENV=production
FLASK_DEBUG=False

# =============================================================================
# 선택적 설정들
# =============================================================================

# Google Translate API (선택적 - googletrans 무료 버전 기본 사용)
GOOGLE_TRANSLATE_API_KEY=your-google-translate-api-key
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# 수집 및 필터링 설정 (기본값 있음)
COLLECTION_INTERVAL_HOURS=6
MAX_ARTICLES_PER_SOURCE=20
MIN_UPVOTES=5
MIN_SCORE_THRESHOLD=70
FINAL_ARTICLE_COUNT=10
SUMMARY_MAX_SENTENCES=3

# 로깅 설정
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# =============================================================================
# API 키 취득 가이드
# =============================================================================

# 1. Reddit API 키 (무료):
#    - https://www.reddit.com/prefs/apps 방문
#    - "Create App" 클릭
#    - 이름: DS News Aggregator
#    - 타입: script
#    - description: 개인 뉴스 수집용
#    - redirect URI: http://localhost (사용 안함)
#    - CLIENT_ID: 앱 이름 아래 문자열
#    - CLIENT_SECRET: secret 문자열

# 2. Google Gemini API 키 (유료지만 저렴):
#    - https://makersuite.google.com/app/apikey 방문  
#    - Google 계정으로 로그인
#    - "Create API key" 클릭
#    - 프로젝트 선택 또는 새로 생성
#    - API 키 복사

# 3. 비용 참고:
#    - Reddit API: 완전 무료
#    - Gemini Pro: 월 15$ 이하 (일반 사용)
#    - googletrans: 무료 (제한적)
#    - Railway: 월 5$ (배포시)
